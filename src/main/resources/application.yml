# set default profile to 'dev'
#spring.profiles.active: default
# On the server, you can then manually override the configuration profile by providing
# a -Dspring.profiles.active=prod parameter to the JVM.
# Alternatively, you can also set your OS’s environment variable to the desired default profile.

# all paths will have prefix /api/
logging.level.com.github.gadzooks: debug

# FIXME : dont use default profile, use In memory mongo for Testing, localhost for dev
# mongodb external for qa and prod profiles
#spring.data.mongodb:
#  host: localhost
#  port: 27017
#  authentication-database: admin
#  username: myUserAdmin
#  #  FIXME : move this to ENV or something
#  password: abc123
#  database: regions-dev
file-paths:
  regionsFilePath: src/main/resources/regions.yml
  locationsFilePath: src/main/resources/locations.yml

# Example : get detailed logs for org.springframework.data.repository.config Package
#logging.level:
#  org:
#    springframework:
#      data:
#        repository:
#          config: debug

spring:
  h2:
    console:
      enabled: true
  datasource:
    generate-unique-name: false
    url: jdbc:h2:mem:testdb
